#include <stdio.h>
int main ()
{
    while (low<high)
    {
        low=0,high=max;
        mid=(low+high)/2;
        if(key==a[mid])  return mid;
        else if (key<mid)  high=mid-1;
        else low=mid+1;
    }
    return 0;
}
